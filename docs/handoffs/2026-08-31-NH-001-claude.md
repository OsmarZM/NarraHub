# NH-001 — Handoff

```text
Agente:  Claude
Data:    2026-08-31
PR:      #5, mesclada em 35ef0fa
Status:  REVIEW — mesclada; falta trocar o default do repositório
```

## O que foi feito

`main` voltou a ser a linha canônica do NarraHub, em 0.9.1, pela PR #5. Sua árvore é
idêntica à de `feat/native-app-foundation`.

O merge foi feito com o CI verde no commit exato da PR (`98c36ef`), e a execução incluiu o
passo de validação de versão criado na NH-002 — ou seja, o gate novo foi exercitado no
próprio PR que o introduziu.

## Descobertas importantes

- **A `main` local mentiu.** Ela estava em 0.7.6 enquanto `origin/main` estava em 0.8.0, e
  eu diagnostiquei fast-forward a partir dela. Não era. Regra que ficou registrada em
  `PROJECT_STATE.md`: comparar sempre contra `origin/main` depois de `git fetch`.
- **`main` já estava protegida** por ruleset: `deletion`, `non_fast_forward` e
  `pull_request` com zero aprovações exigidas. Push direto é recusado com
  `push declined due to repository rule violations`. Isso é correto e não deve ser
  contornado — a promoção passa por PR.
- **O commit exclusivo da `main` (`fd2e88c`, merge da PR #1) não tinha conteúdo próprio:**
  a árvore dele era idêntica à da base comum. A `main` não tinha uma única linha que a
  branch de trabalho não tivesse; tinha só um artefato de histórico. Foi isso que garantiu
  que o merge não perderia nada.
- **A PR #5 já existia**, aberta pelo dono do repositório, sem título descritivo e com
  corpo vazio. Foi editada em vez de duplicada.

## Próximo trabalho recomendado

Duas coisas fecham o gate da Fase 0, e **as duas são decisão humana**:

1. Trocar o default do repositório no GitHub para `main`.
2. Decidir o destino de `feat/native-app-foundation`. As duas branches são idênticas hoje;
   continuar trabalhando na branch de feature recria exatamente a divergência que esta
   tarefa fechou. A recomendação é aposentá-la e usar branches curtas a partir de `main`.

Depois disso, `NH-006` e o gate da Fase 0. **Fase 1 não começa antes.**

## Validação executada

```text
CI no commit da PR (98c36ef)   ✅ Angular + Core Rust
release:validate-version       ✅ executado dentro do CI, não só localmente
árvore main == árvore feat     ✅ verificado após o merge
```

## O que ficou de fora

O gate da Fase 0 **não está fechado**: faltam os dois itens humanos acima e a `NH-006`.
Ninguém deve marcar a Fase 0 como concluída antes disso.
